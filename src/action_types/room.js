import {keyMirror} from './key_mirror';

export default keyMirror({
    INITIALIZE_ROOM: null,
    PERMISSIONS: null,
    MEETING_STARTED: null,
    JOIN_REQUEST:null,
    ACCEPT_REJECT:null,
    JOINED_ROOM:null,
    CLOSE_MEETING:null,
});